{
  "name": "tapiocabot",
  "version": "1.0.0",
  "description": "Telegram BOT to register tapioca orders",
  "main": "index.js",
  "author": "Renan Bandeira",
  "license": "MIT",
  "dependencies": {
    "firebase": "^7.6.1",
    "node-telegram-bot-api": "^0.40.0"
  },
  "scripts": {
    "start": "node src/index.js",
    "build": "rm public/.keep && cp -R src/* public && node public/index.js"
  },
  "devDependencies": {
    "@now/node": "^1.3.0",
    "@now/node-server": "^0.8.2"
  }
}
